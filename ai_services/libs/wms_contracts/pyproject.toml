[project]
name = "wms-contracts"
version = "0.1.0"
description = "Shared schemas and WMS API client"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.8",
  "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
