# ── Core data science ──────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # required for pd.read_parquet()

# ── ML models (used in 03_model_comparison_m5.ipynb) ──────────────────────
scikit-learn>=1.3      # RandomForestRegressor
xgboost>=2.0           # XGBRegressor
catboost>=1.2          # CatBoostRegressor
lightgbm>=4.0          # LGBMRegressor

# ── Statistical / classical models ─────────────────────────────────────────
statsmodels>=0.14
pmdarima>=2.0          # auto_arima (SARIMA cell in notebook)
prophet>=1.1           # Prophet (optional — slow to install, skip if not needed)

# ── Visualization ──────────────────────────────────────────────────────────
matplotlib>=3.7
seaborn>=0.12
# ── Notebook environment ───────────────────────────────────────────────────
jupyter>=1.0
ipykernel>=6.0

# ── New Dependencies ───────────────────────────────────────────────────────
openpyxl>=3.1
pyyaml>=6.0
mlflow==2.16.0
deap>=1.3.3
missingno>=0.5.2
pulp>=2.8.0
optuna>=3.0
pmdarima>=2.0
hierarchicalforecast>=0.4
lightgbm>=4.0
